{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run C++ (MinGW)",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/1stuff/${fileBasenameNoExtension}.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}/1stuff/",
            "environment": [],
            "externalConsole": true,
            "MIMode": "gdb",
            "setupCommands": [
                {
                    "description": "Enable pretty-printing for gdb",
                    "text": "-enable-pretty-printing",
                    "ignoreFailures": true
                }
            ],
            "preLaunchTask": "Compile C++"
        }
    ]
}
